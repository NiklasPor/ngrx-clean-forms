{
    "scripts": {
        "start": "ng serve",
        "build": "cp README.md projects/example-app/README.md && ng build && rm projects/example-app/README.md",
        "test": "ng test",
        "lint": "ng lint"
    }
}
