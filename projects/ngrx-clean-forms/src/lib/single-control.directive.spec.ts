import { SingleControlDirective } from './single-control.directive';

describe('SingleControlDirective', () => {
  it('should create an instance', () => {
    const directive = new SingleControlDirective();
    expect(directive).toBeTruthy();
  });
});
